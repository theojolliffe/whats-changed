import { FuseResultWithMatches } from 'fuse.js';
declare function formatFuseJs<T>(results: FuseResults<T>): FormattedResults<T>;
declare type FuseResults<T> = ReadonlyArray<FuseResultWithMatches<T>>;
declare type FormattedResults<T> = (T | IFormatted)[];
interface IFormattedResult {
    text: string;
    matches: boolean;
}
interface IFormatted {
    [key: string]: IFormattedResult[];
}
export default formatFuseJs;
